export interface credentialsType { 
    correoElectronico: string,
    contrasena: string 
}

export interface UserType {
    id: string;
    nombre: string;
    email: string;
    imagen?: string;
}
